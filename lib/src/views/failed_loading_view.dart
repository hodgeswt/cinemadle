import 'package:flutter/material.dart';

class FailedLoadingView extends StatelessWidget {
  const FailedLoadingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Failed loading"));
  }
}
